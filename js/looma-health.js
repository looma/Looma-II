/*
 * filename: looma-health.js
 * First Aid module — HOME grid page. Activates the Health toolbar button.
 * Card navigation is plain <a href> links, so it works with no network.
 */
'use strict';

$(document).ready(function () {
    toolbar_button_activate("health");
});
